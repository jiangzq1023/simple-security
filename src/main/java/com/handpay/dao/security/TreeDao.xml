<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.handpay.dao.security.TreeDao">  
    
    <!-- 查询全部菜单 -->  
    <select id="findAll" resultType="com.handpay.entity.security.Tree" >  
            select * from security_tree 
    </select>
    <!-- 查询用户拥有的全部菜单 -->  
    <select id="findAllResourcesByUserName" parameterType="String" resultType="com.handpay.entity.security.Tree" >  
            select * from security_tree 
    </select>
</mapper> 